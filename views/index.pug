extends layout

block content
    - function getGetOrdinal(n){var s=["th","st","nd","rd"];var v=n % 100;return n+(s[(v-20)%10]||s[v]||s[0])}
    p(id='month')= data.date.split(" ")[0].toUpperCase()
    p(id='day') #{getGetOrdinal(data.date.split(" ")[1].replace(",", ""))}
